spring:
  application:
    name: customer
  cloud:
    config:
      uri: http://localhost:9101
      fail-fast: true
      retry:
        initial-interval: 1500
        multiplier: 1.5
        max-attempts: 10000
        max-interval: 1000
